#!/usr/bin/perl -w
use strict;
use utf8;
use open qw(:std :utf8);

print STDERR "## LOG ($0): stdin expected ...\n";
while(<STDIN>) {
    chomp;
    m/^(\S+)\s+(.*)/g or next;
    print "$1\t$1\n";
}
print STDERR "## LOG ($0): stdin ended ...\n";
